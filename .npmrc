# Required: @browserbasehq/stagehand → openai@4.x declares peerOptional zod@^3.23.8
# but this project uses zod@^4. The conflict is in a nested dep and cannot be fixed
# by pinning. Remove this flag once stagehand ships openai@5+ or zod@4 peer support.
legacy-peer-deps=true